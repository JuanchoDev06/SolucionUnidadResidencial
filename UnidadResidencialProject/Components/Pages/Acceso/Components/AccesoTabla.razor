@using UnidadResidencialProject.Models

<div class="tabla-wrap">
    <table class="acceso-table">
        <thead>
            <tr>
                <th>#</th>
                <th>Tipo</th>
                <th>Nombre</th>
                <th>Documento</th>
                <th>Unidad</th>
                <th>Vigilante</th>
                <th>Ingreso</th>
                <th>Salida</th>
                <th>Estado</th>
                <th>Acción</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Ingresos)
            {
                <tr>
                    <td class="td-id">@item.IngresoId</td>
                    <td><span class="tipo-tag @item.Tipo.ToLower()">@item.Tipo</span></td>
                    <td class="td-nombre">@item.NombrePersona</td>
                    <td class="td-mono">@item.Documento</td>
                    <td>@(item.Unidad?.Numero ?? "—")</td>
                    <td>@(item.Vigilante?.Nombre ?? "—")</td>
                    <td class="td-mono">@item.FechaHoraIngreso.ToString("hh:mm tt")</td>
                    <td class="td-mono">@(item.FechaHoraSalida?.ToString("hh:mm tt") ?? "—")</td>
                    <td>
                        @if (item.FechaHoraSalida == null)
                        {
                            <span class="estado-tag dentro">Dentro</span>
                        }
                        else
                        {
                            <span class="estado-tag salio">Salió</span>
                        }
                    </td>
                    <td>
                        @if (item.FechaHoraSalida == null)
                        {
                            <button class="btn-salida" @onclick="@(() => OnRegistrarSalida.InvokeAsync(item.IngresoId))">
                                Registrar Salida
                            </button>
                        }
                        else
                        {
                            <span class="td-ok">✓</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@code {
    [Parameter] public List<IngresoDto> Ingresos { get; set; } = new();
    [Parameter] public EventCallback<int> OnRegistrarSalida { get; set; }
}